{
    "devDependencies": {
        "esbuild": "^0.20.2",
        "vitest": "^1.5.2"
    },
    "scripts": {
        "build": "./node_modules/.bin/esbuild src/main.ts --bundle --outdir=build",
        "test": "vitest"
    }
}